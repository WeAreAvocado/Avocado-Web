import React from 'react';
import styles from "@/components/button/Button.module.css"

const Button: React.FC = () => {
  return (
    <button className={styles.gsbutton}>
      Get Started
    </button>
  );
};

export default Button;
